# .bat files must be CRLF — cmd.exe batch label resolution is unreliable
# with LF-only endings (silent "label not found" even when the label exists).
# Auto detect text files and perform LF normalization
* text=auto
*.sh text eol=lf
*.py text eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf